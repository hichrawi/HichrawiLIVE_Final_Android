<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <application
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="false" android:supportsRtl="true"
        android:icon="@drawable/hichrawi_live_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.HichrawiLIVE">
        <activity android:name=".PlayerActivity" android:screenOrientation="landscape" />
        <activity android:name=".ChannelsActivity" android:screenOrientation="unspecified" />
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
